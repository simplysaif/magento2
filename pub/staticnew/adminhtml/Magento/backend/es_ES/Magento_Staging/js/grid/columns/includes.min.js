define(['underscore','Magento_Ui/js/grid/columns/column'],function(_,Column){'use strict';return Column.extend({defaults:{bodyTmpl:'Magento_Staging/grid/includes'},countItems:function(record){var items=this.getItems(record),total=0;items.forEach(function(item){total+=parseInt(item.count,10);});return total;},getItems:function(record){return record[this.index];},getItemLabel:function(item){return item.entityLabel;}});});