define(['dmpt','domReady!'],function(_dmTrack){'use strict';function init(items,total){var fLen=items.length,i=0;for(i;i<fLen;i++){_dmTrack('product',items[i]);}
_dmTrack('CheckOutAmount',total);}
return function(roiCode){if(roiCode.isEnabled){init(roiCode.items,roiCode.total);}};});